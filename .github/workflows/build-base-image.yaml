---
name: Build Base Image

on:
  push:
    paths:
      - docker/**
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:
    inputs:
      release_candidate:
        description: Manually rebuild base image.
        required: true

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
      - name: Set env with tag
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        run: |
          export RELEASE_VERSION=${GITHUB_REF#refs/tags/}
          echo "RELEASE_VERSION=$RELEASE_VERSION" >> $GITHUB_ENV
          echo "DOCKER_TAGS="colmpc:base-$RELEASE_VERSION, colmpc:base-latest"" >> $GITHUB_ENV
      - name: Set env with tag
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        run: |
          echo "RELEASE_VERSION=""" >> $GITHUB_ENV
          echo "DOCKER_TAGS="colmpc:base-latest"" >> $GITHUB_ENV

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/base.Dockerfile
          tags: ${{ env.DOCKER_TAGS }}
          push: false

      - name: Login to GitHub Container Registry
        # Push the latest image to registry only if running on main
        if: ${{ (github.ref == 'refs/heads/main') || startsWith(github.event.ref, 'refs/tags/v') }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Docker image
        uses: docker/build-push-action@v6
        if: ${{ (github.ref == 'refs/heads/main') || startsWith(github.event.ref, 'refs/tags/v') }}
        with:
          tags: ${{ env.DOCKER_TAGS }}
          push: true
